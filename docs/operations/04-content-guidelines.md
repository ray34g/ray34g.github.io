# コンテンツ更新ルール

## 命名
- コンテンツは `content/ja`, `content/en` に分ける。
- slug は言語間で可能な限り揃える。

## 画像
- 画像は用途別に配置し、無秩序に直下へ置かない。
- 代替テキストを必ず設定する。
- 可能な限り軽量形式（webp等）を使う。

## 翻訳
- UI文言は `i18n/*.toml` で管理する。
- `data/**/*.toml` の `i18n_key` は ja/en 両方に定義する。
- 翻訳追加時は `./ci/scripts/check_i18n_keys.sh` で確認する。
