# 言語別コンテンツ運用ルール

- 日本語コンテンツは `content/ja`、英語コンテンツは `content/en` に配置する。
- 可能な限り同一slugを維持し、翻訳対応ページを揃える。
- 共通UI文言は `i18n/*.toml` のキー参照を必須とする。
- ナビゲーションやCTAなどの構造データは `data/` に集約する。
- ページ固有文言はfront matterで持ち、共通語彙との重複を避ける。
